User-agent: *
Allow: /
# legacy/ is the archived previous portfolio. GitHub Pages serves the whole
# repo, so it is reachable — keep it out of the index so it never competes
# with the live page in search results.
Disallow: /legacy/

# bundle/ is the raw Claude Design export that build.mjs reads. It has to
# stay in the repo as the build input, but it is 900+ KB of unrendered
# template source and must never be indexed as if it were the site.
Disallow: /bundle/

Sitemap: https://saifxss.github.io/sitemap.xml

# The rebuilt site, parked here while it is finished. Not the front door yet.
Disallow: /preview.html
